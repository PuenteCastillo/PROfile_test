import { EditableTextField } from "globals/types";

type SocialMediaFeedBrickProps = {
  title: EditableTextField;
  subTitle: EditableTextField;
};

type FeedHeaderProps = {
  titleSize: number;
  subTitleSize: number;
};

type FeedTitleProps = {
  titleSize: number;
};

type FeedSubTitleProps = {
  subTitleSize: number;
};
